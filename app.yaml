runtime: python311
env: standard
instance_class: F1

entrypoint: gunicorn web_server:app --bind 0.0.0.0:$PORT --workers 2 --threads 4 --timeout 120

env_variables:
  FLASK_ENV: "production"

handlers:
  - url: /static
    static_dir: static

  - url: /.*
    script: auto

