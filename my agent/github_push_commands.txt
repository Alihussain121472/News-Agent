# Commands to push AI News Agent Portfolio to GitHub

# Remove old remote (if exists)
git remote remove origin

# Add new remote repository
git remote add origin https://github.com/Alihussain121472/ai-news-agent-portfolio.git

# Push to GitHub
git push -u origin main

# Verify it was pushed
git remote -v
