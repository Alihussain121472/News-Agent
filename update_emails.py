import re

with open('ai_news_agent.py', 'r', encoding='utf-8') as f:
    text = f.read()

# I will rewrite the entire email formatting section. 
# It's better to just write a function that replaces the whole block of functions or use regex to replace each function.

# Let's write a python script to cleanly replace the email functions.
